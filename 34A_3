#include <stdio.h>
#include <stdlib.h>
int main()
{
  float side, area;
  printf("Enter the length of side of a square: ");
  scanf("%f", &side);
  area=side*side;
  printf("The area of the square is: %.2f", area);
}
