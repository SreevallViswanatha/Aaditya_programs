#include <stdio.h>
#include <stdlib.h>

int main()
{
    float side, perimeter;
    printf("Enter one side's length of a square in cm: ");
    scanf("%f", &side);
    perimeter=4*side;
    printf("The perimeter of the square with side %.2f is: %.2f", side, perimeter);
}
